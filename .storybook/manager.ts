import { addons } from 'storybook/manager-api';

addons.setConfig({
  theme: {
    base: 'light',
    brandTitle: 'Orbit HR Design System',
    brandImage: '/orbit-logo.png',
    brandUrl: '/',
  },
});
